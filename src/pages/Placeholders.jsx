import React from 'react';

const Projects = () => (
    <div className="min-h-screen flex items-center justify-center">
        <h1 className="text-4xl font-display font-semibold text-ink">Projects Coming Soon</h1>
    </div>
);

const Resume = () => (
    <div className="min-h-screen flex items-center justify-center">
        <h1 className="text-4xl font-display font-semibold text-ink">Resume Coming Soon</h1>
    </div>
);

const Blogs = () => (
    <div className="min-h-screen flex items-center justify-center">
        <h1 className="text-4xl font-display font-semibold text-ink">Blogs Coming Soon</h1>
    </div>
);

export { Projects, Resume, Blogs };
